# C.19 acceptance and release record

Read the latest receipt in [source PR #89](https://github.com/mbace1/piritori-eden/pull/89), [hub development #546](https://github.com/mbace1/Suds-Jack/pull/546) and [Pages #547](https://github.com/mbace1/Suds-Jack/pull/547). Those records distinguish tested heads, merges and the actual public-site check. A source commit, successful pure tests or a screenshot is not by itself a public release or physical-device approval.

## Findings corrected before release

1. Initial browser run 35266445631 on 595d380 reached focused/all-stat views, but the crew drawer intercepted target taps. Fighter selection and action choice now close it, update aria-expanded and restore focus from hidden controls. Real touch/controller tests require this; forced taps were not used to hide it.
2. A once-helped colleague or kit carrier who falls again must not receive an impossible second-Help instruction. Both branches now say rescue is unavailable for this outing; first-down, alive/escorting and extracted states have explicit presentation-only regression fixtures.
3. The laboratory build title/header/Help now agree on C.19; its unchanged C.11 tactical resolver is identified separately. External character work remains on hold.
4. The published `/web/crew-run/c17/` bookmark remains a generated redirect to the current canonical campaign entry, with its own receipt hash. The packager no longer silently removes it.
5. The publisher derives build identity instead of hardcoding C.16.1, and covers generated entry/version files in the final receipt. Hub acceptance checks the actual candidate rather than an old C.18 checkout. Offline hub shell generation belongs to both matching hub PRs.

The defeat-reload test compares every JSON-persisted value. The earlier assertion mismatch involved optional undefined properties that JSON omits; it was a harness mismatch, not evidence of missing saved data. No health/state injection or direct session command is used in the browser defeat route.

## Evidence and acceptance boundaries

Packaged browser run 35267900815 on 792e7bd passed portrait/landscape labels and free target inspection, actual five-round defeat, mid-mission reload, once-only aftermath, onward recovery deployment, and non-overlapping 2/6/12 label fixtures. Screenshots were inspected. This is earlier-head evidence, not a substitute for the final corrected head's gates.

Before merge/publication, require the final source's existing gates, C18 renderer review and C19 packaged acceptance; the final hub candidates' exact-package/actual-entry/offline-shell checks; and inspection of screenshots. After Pages deployment, require the real public hub route and all shipping-file hashes to match the tested receipt. Preserve C09 rollback and all unrelated games.

**OPEN:** physical Pixel 10 Pro / iPad M2 rendering and owner visual/play acceptance. Godot presentation is a documented port handoff, not an implementation. Emulated Chromium/SwiftShader is not physical-device evidence.

Map and mission expansion are not implemented by C.19. Continue [POST_C18_ITERATION_PLAN.md](POST_C18_ITERATION_PLAN.md) through small, revisitable playable steps. Follow [C19_READABILITY.md](C19_READABILITY.md) for behavior and the Godot boundary. Keep external models, additional arenas and combat-rule changes parked.
